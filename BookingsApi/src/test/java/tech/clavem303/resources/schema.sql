CREATE TABLE vehicle (
                         id BIGINT PRIMARY KEY,
                         brand VARCHAR(255),
                         model VARCHAR(255),
                         year INT,
                         status VARCHAR(50)
);